# UNO R4 WiFi Board Implementation

Pending: source-backed LED source/sink mappings, high-impedance switching, timer configuration, and run-description export.

Keep board-specific GPIO/register operations in this directory. Do not vendor the entire Arduino core. The board identifier is `uno_r4_wifi`, matching `profiles/boards/uno_r4_wifi.json`.
